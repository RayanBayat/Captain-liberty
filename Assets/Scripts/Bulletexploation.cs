using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Bulletexploation : MonoBehaviour
{
    // Start is called before the first frame update
    //Even parry uses this script

    void Destroyobject()
    {

        Destroy(gameObject);
        
    }
}
