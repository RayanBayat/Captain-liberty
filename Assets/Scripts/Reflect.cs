using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Reflect : MonoBehaviour
{
    // Start is called before the first frame update
    // public Weapen weapen;
    // public Bullet bullet;
   // [SerializeField] private LayerMask Enemiesbullets;

    // Update is called once per frame


    void Update()
    {
         
    }

    // void reflect_on()
    // {
    //     transform.tag = "reflect";
    // }
    // void reflect_off()
    // {
    //     transform.tag = "Untagged";
    // }
    // private void OnCollisionEnter2D(Collision2D other) {

    //    Debug.Log("block");
    //     if (weapen.shieldtimer < 0.5 && other.gameObject.layer == 10)
    //     {
    //         other.transform.localRotation = Quaternion.Euler(transform.localRotation.x,0.0f,transform.localRotation.z);
    //     }
    // }
    // private void OnTriggerEnter2D(Collider2D other) {

      
    //     if (weapen.shieldtimer < 0.5 && other.gameObject.layer == 10)
    //     {
    //         other.transform.position = (new Vector3(0,0,0));
    //       //  other.ga
    //          Debug.Log("block");
    //    //     other.gameObject.GetComponent<Rigidbody2D>().velocity = other.transform.right * other.
    //      //   other.transform.localRotation = Quaternion.Euler(transform.localRotation.x,0.0f,transform.localRotation.z);
    //     }
    // }
}
